package fr.siovision.voyages.domain.model;

/** Champs d'état civil non modifiables en self-service (RGPD Art. 16 — demande soumise à l'ADMIN). */
public enum RectificationField {
    FIRST_NAME, LAST_NAME, BIRTH_DATE, EMAIL
}
