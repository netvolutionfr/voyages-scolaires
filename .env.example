SPRING_APPLICATION_NAME=voyages

# use dev for development (prod or nothing for prod)
SPRING_PROFILES_ACTIVE=

# Connexion à PostgreSQL
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/voyages
SPRING_DATASOURCE_USERNAME=voyages
SPRING_DATASOURCE_PASSWORD=SECRET_PASSWORD
SPRING_DATASOURCE_DRIVER-CLASS-NAME=org.postgresql.Driver

# Configuration JPA / Hibernate
SPRING_JPA_HIBERNATE_DLL-AUTO=update
SPRING_JPA_SHOW-SQL=true
SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT=org.hibernate.dialect.PostgreSQLDialect

SPRINGDOC_DEFAULT-PRODUCES-MEDIA-TYPE=application/json
SPRINGDOC_DEFAULT-CONSUMES-MEDIA-TYPE=application/json

SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER-URI=

LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_SECURITY=DEBUG

KEYCLOAK_SERVER_URL=
KEYCLOAK_REALM=
KEYCLOAK_CLIENT_ID=
KEYCLOAK_CLIENT_SECRET=

APP_FRONTEND_URL=

# Configuration S3
S3_BUCKET=voyages
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_PRESIGN_MIN=10

ACTIVATION_JWT_SECRET=une-cle-ultra-secrete-tres-longue
ACTIVATION_ISSUER=campusaway
ACTIVATION_EXPIRY_MINUTES=30
FRONTEND_ACTIVATION_URL=http://localhost:5173/activate

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_FROM=Sender Name <sender@example.com>
SMTP_USER=smtp-user
SMTP_PASS=smtp-pass

WEBAUTHN_RP_ID=https://campusaway.fr
WEBAUTHN_RP_NAME=Campus Away
WEBAUTHN_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8000,https://campusaway.fr
WEBAUTHN_CHALLENGE_TTL_SECONDS=300

JWT_ISSUER=https://campusaway.fr
JWT_ACCESS_TTL_SECONDS=3600

JWT_PENDING_TTL_SECONDS=900
JWT_SECRET_KEY=<HS256 or path to RSA/EC keys> # openssl rand -base64 32
OTP_TTL_SECONDS=600
OTP_LENGTH=6